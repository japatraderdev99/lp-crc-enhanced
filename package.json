{
  "name": "lp-crc-optimized",
  "version": "2.0.0",
  "description": "Landing Page CRC otimizada - HTML/CSS/Vanilla TypeScript",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.js --dev",
    "build": "node esbuild.config.js",
    "preview": "npx serve dist -p 3000"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "esbuild-plugin-copy": "^2.1.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {}
}
